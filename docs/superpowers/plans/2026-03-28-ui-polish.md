# UI Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix the clipped preference row, tighten the logged-out Home empty state, and reduce Settings summary density on phone screens.

**Architecture:** Reuse shared UI primitives instead of adding page-local workarounds. Put the safe-area fix in `PrefsScreen`, keep the Home change inside `EmptyScreen`, and treat Settings readability as a localized copy update rather than a logic change.

**Tech Stack:** Kotlin, Jetpack Compose, Android resource strings, Gradle assemble + emulator QA

---

### Task 1: Preference Screen Bottom Safe Area

**Files:**
- Modify: `app/src/main/java/com/huanchengfly/tieba/post/ui/common/prefs/PrefsScreen.kt`

- [ ] Add bottom `contentPadding` support to `PrefsScreen`.
- [ ] Merge the supplied bottom padding with navigation bar insets.
- [ ] Keep existing call sites working without additional changes.

### Task 2: Home Empty State Tightening

**Files:**
- Modify: `app/src/main/java/com/huanchengfly/tieba/post/ui/page/main/home/HomePage.kt`

- [ ] Reduce the compact-screen visual weight of the logged-out empty-state illustration.
- [ ] Keep the title, message, and actions unchanged in behavior.
- [ ] Avoid touching unrelated Home page list or loading logic.

### Task 3: Settings Summary Readability

**Files:**
- Modify: `app/src/main/res/values/strings.xml`
- Modify: `app/src/main/res/values-b+zh+Hant/strings.xml`
- Modify: `app/src/main/res/values-en/strings.xml`

- [ ] Shorten the settings informational summaries without changing meaning.
- [ ] Keep all three locales aligned.
- [ ] Leave feature gating and summary generation logic intact.

### Task 4: Verification

**Files:**
- No tracked code files required

- [ ] Run `export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64; ./gradlew :app:assembleDebug --console=plain`.
- [ ] Install the debug build on the emulator.
- [ ] Verify the Personalization last item is fully visible above the gesture area.
- [ ] Verify the Home logged-out empty state feels tighter on phone width.
- [ ] Verify the top of Settings reads more cleanly on phone width.
